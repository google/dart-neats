# Changelog

## 0.0.1 (Unreleased)

- Initial development version of `checks_ext` containing extensions for `package:checks`.
- Added extensions for standard library types (`DateTime`, `Duration`, `RegExp`, `Match`, `Iterable`, `String`, `num`, and `List<int>` bytes).
